name: domain
description: RadioFlow domain layer — entities and repository contracts (pure Dart).
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  equatable: ^2.0.7
  freezed_annotation: ^3.0.0

dev_dependencies:
  build_runner: ^2.4.13
  freezed: ^3.0.0
  lints: ^6.0.0
  test: ^1.25.15
