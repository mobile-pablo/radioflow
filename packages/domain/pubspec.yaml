name: domain
description: RadioFlow domain layer — entities and repository contracts (pure Dart).
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  equatable: ^2.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
