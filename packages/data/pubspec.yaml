name: data
description: RadioFlow data layer — Radio Browser API client, DTOs, mappers, and repositories.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.6.0
  flutter: ">=3.24.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
